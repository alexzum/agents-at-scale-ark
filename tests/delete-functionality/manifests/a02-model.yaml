apiVersion: ark.mckinsey.com/v1alpha1
kind: Model
metadata:
  name: test-delete-model
  namespace: default
spec:
  type: azure
  model:
    value: gpt-4
  config:
    azure:
      baseUrl:
        value: "https://test.openai.azure.com/"
      apiKey:
        valueFrom:
          secretKeyRef:
            name: test-delete-secret
            key: api-key
      apiVersion:
        value: "2024-12-01-preview"
