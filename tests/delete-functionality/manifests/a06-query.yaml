apiVersion: ark.mckinsey.com/v1alpha1
kind: Query
metadata:
  name: test-delete-query
  namespace: default
spec:
  target:
    type: agent
    name: test-delete-agent
  input:
    value: "Hello, this is a test query for delete functionality testing."
