apiVersion: ark.mckinsey.com/v1alpha1
kind: Team
metadata:
  name: test-delete-team
  namespace: default
spec:
  strategy:
    type: sequential
  members:
    - name: test-delete-agent
      role: primary
